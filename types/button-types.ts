export interface ButtonProps {
  text: string;
  textColor: string;
  href: string;
  borderColor: string;
  borderWidth: number;
  borderRadius: string;
  bgColor: string;
  hoverBgColor: string;
  shadow: boolean;
  className?: string;
}
